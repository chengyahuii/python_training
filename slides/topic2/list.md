# 列表

```py
# A list of integers
L = [1, 2, 3]

# A list of strings
L = ['red', 'green', 'blue']

# nested list
L = ['a', ['bb', ['ccc', 'ddd'], 'ee', 'ff'], 'g', 'h']

# Get data from list
for i in L:
    print(i)

# Get data with index from list
L[0]
```
