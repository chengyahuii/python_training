# Class

```
类和对象是面向对象编程的两个主要方面。

类是创建单个对象的蓝图。

例如，在现实世界中，可能有成千上万的汽车存在，都是相同的品牌和型号。
每辆汽车都是根据同一套蓝图建造的，因此包含相同的组件。在面向对象的术语中，车是Car类（蓝图）的一个实例（对象）。

在 Python 中，所有东西都是一个对象--整数、字符串、列表、函数，甚至类本身。

例如，当你输入num = 42时，Python实际上创建了一个新的整数类型的对象，其值为42，并将其引用分配给num这个名字。
```